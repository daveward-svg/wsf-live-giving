import{initializeApp}from'https://www.gstatic.com/firebasejs/11.0.2/firebase-app.js';import{getDatabase,ref,onValue}from'https://www.gstatic.com/firebasejs/11.0.2/firebase-database.js';import{firebaseConfig,campaignPath}from'./firebase-config.js';const app=initializeApp(firebaseConfig),db=getDatabase(app),r=ref(db,campaignPath),money=new Intl.NumberFormat('en-US',{style:'currency',currency:'USD',maximumFractionDigits:0});onValue(r,s=>{const d=s.val()||{raised:0,goal:10000,name:'Rainforest Falls',subtitle:'The Nature of God • VBS 2026',givingUrl:'https://wsfirst.com/'};const p=Math.min(100,(d.raised/d.goal)*100);name.textContent=d.name;subtitle.textContent=d.subtitle;raised.textContent=money.format(d.raised);goal.textContent=money.format(d.goal);percent.textContent=`${p.toFixed(0)}% complete`;remaining.textContent=`${money.format(Math.max(0,d.goal-d.raised))} remaining`;fill.style.width=`${p}%`;give.href=d.givingUrl;message.textContent=p>=100?'Goal reached! Thank you, Winston Salem First!':p>=75?'The rainbow is shining—our goal is within reach!':p>=50?'Halfway there! Thank you for helping VBS come to life.':p>=25?'We reached our first milestone. Keep it going!':"Every gift helps children discover the wonder of God's creation.";status.textContent='Live from Firebase';updated.textContent=new Date().toLocaleTimeString()});
